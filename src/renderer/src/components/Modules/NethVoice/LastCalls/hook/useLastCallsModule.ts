import { useLoggedNethVoiceAPI } from "@renderer/hooks/useLoggedNethVoiceAPI"
import { useStoreState } from "@renderer/store"
import { ContactType, PhonebookModuleData, NethLinkPageData, PhonebookSearchModuleData, SearchCallData, SearchData, SelectedContact, StateType } from "@shared/types"
import { log } from "@shared/utils/logger"

export const useLastCallsModule = (): {

} => {

  return {

  }

}
